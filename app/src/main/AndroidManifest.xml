<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.demo">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:name="com.example.demo.MyApplication"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.DemoApplication"
        android:usesCleartextTraffic="true">
        <activity
            android:name="com.example.demo.ui.MainActivity"
            android:label="@string/app_name"
            android:theme="@style/Theme.DemoApplication.NoActionBar"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity android:name="com.example.demo.ui.activity.SecondActivity" />

        <activity android:name="com.example.demo.ui.activity.ItemTouchHelperActivity" />

        <activity
            android:name="com.example.demo.ui.activity.WebViewActivity"
            android:theme="@style/Theme.DemoApplication.FullScreen"
            android:windowSoftInputMode="adjustResize"
            android:windowTranslucentStatus="true"
            android:windowTranslucentNavigation="true"
            />

        <activity android:name="com.example.demo.ui.activity.PopupwindowActivity" />
        <activity android:name="com.example.demo.ui.activity.ViewPager2TestActivity" />
        <activity android:name="com.example.demo.ui.activity.ViewMockHttpTestActivity" />

    </application>

</manifest>